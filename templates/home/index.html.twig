{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <a class="btn btn-info" href="{{ path('app_category')}}">Categories</a>
    <a class="btn btn-info" href="{{ path('category_add')}}">Add category</a>
    <a class="btn btn-info" href="{{ path('app_register')}}">Register</a>
    <a class="btn btn-info" href="{{ path('admin_register')}}">Admin Register</a>
    <a class="btn btn-info" href="{{ path('app_login')}}">Login</a>
</div>
{% endblock %}
